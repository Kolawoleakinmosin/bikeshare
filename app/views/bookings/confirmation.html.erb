
<div class="container mt-3 bg-white p-4 round shadow" style="width: 30rem;">
  <h2 class="mb-3">Booking Confirmed!</h2>
  <p class="mb-5">These Are Your Booking Details:</p>
    <h4 class="mb-4"><%= @booking.bike.title %></h4>
    <h6>Start Date: <%= @booking.start_time.strftime("%b %d, %Y") %></h6>
    <h6 class="mb-4">End Date: <%= @booking.end_time.strftime("%b %d, %Y") %></h6>
    <h5 class="mb-4"><strong><%= number_to_currency(@booking.total_sum, unit: "£") %></strong></h5>
    <%= link_to "Make Payment", my_bookings_path, class: "btn btn-primary" %>
</div>
